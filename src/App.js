import styles from "./css/App.module.css";

function App() {
  return (
    <div className={`${styles.App}`}>
      <header className={`${styles.AppHeader}`}>
        <h1>LyricLab</h1>
      </header>
    </div>
  );
}

export default App;
